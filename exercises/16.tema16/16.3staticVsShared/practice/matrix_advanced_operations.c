// matrix_advanced_operations.c
#include "matrix_advanced_operations.h"

void matrix_multiply(int *a, int *b, int *result, int rows_a, int cols_a, int cols_b) {
    // Implementación de la multiplicación de matrices
    // (omitiendo detalles para simplificar el ejemplo).
}

void matrix_transpose(int *a, int *result, int rows, int cols) {
    // Implementación de la traspuesta de la matriz
    // (omitiendo detalles para simplificar el ejemplo).
}
